import Layout from '../components/Layout';

export const About = () => {
    return (
        <Layout>
            <section id="about">
                <h1>About</h1>
            </section>
        </Layout>
    );
};

export default About;
