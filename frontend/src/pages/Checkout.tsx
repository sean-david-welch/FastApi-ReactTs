import Layout from '../components/Layout';

export const Checkout = () => {
    return (
        <Layout>
            <section id="checkout">
                <h1>Checkout</h1>
            </section>
        </Layout>
    );
};

export default Checkout;
