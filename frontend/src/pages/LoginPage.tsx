import Layout from '../components/Layout';

export const Login = () => {
    return (
        <Layout>
            <section id="login">
                <h1>Login</h1>
            </section>
        </Layout>
    );
};

export default Login;
